import { FC } from "react";
import { Flex } from '@chakra-ui/react'


const NavBar: FC = () => {

    return (
        <Flex>
            home
            stream
            user
            settings
        </Flex>
    );
}

export { NavBar };